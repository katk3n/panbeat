# Raw evidence

Store each captured run in a new, uniquely named directory. Preserve the input
fixture checksum, environment/version metadata, timestamp with timezone, and
run manifest alongside the raw log. Never overwrite a prior run.
