# Chart fixtures

The generated Phase 0 chart is stored in `../test-pack/chart.json`; its source
parameters are `../test-pack-source.json`. Run `scripts/generate-test-pack
verify` to confirm the checked-in asset and checksums.
