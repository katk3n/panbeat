# MIDI trace fixtures

Validated engine-independent JSON Lines traces belong here. Use
`scripts/midi-trace validate --input PATH`, `summarize`, or `replay`; replay's
`--mode no-wait` output is the common fixture consumed by engine adapters.
